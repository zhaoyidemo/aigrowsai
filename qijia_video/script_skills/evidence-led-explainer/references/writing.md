把内部完成的内容判断直接写成连贯口播，再按真实语义变化拆成 4—8 个叙事段；内容很集中时可以 3 段，复杂论证最多 12 段，不为凑数量切碎句子。

开场立即呈现命题中的矛盾、判断、选择或结果，不用空泛寒暄、虚假悬念、受众焦虑和命令式 CTA。背景只保留会改变理解的内容；每段都必须增加材料、语境、推理或结论。可以使用模型已有的稳定知识，但不得编造精确出处、版本、日期、数据或最新动态。直接引语只有在用户明确核对的材料足以支持时才可按原话处理；否则应写成“这段话表达的观点”或转述，不得伪装成已核验原话。

narration 是唯一送入 TTS 的文本。脚本不写 visual_direction，也不描述画面。on_screen_text 只保留极少量适合后期排版的强调词，不需要时为空。写完后按 critic_rules 做一次内部审稿，把发现的问题直接修入最终稿；只交付 ScriptDraft，不附带计划、分析或候选方案。
